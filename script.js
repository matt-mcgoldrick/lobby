var username = prompt("Enter a username");
var password = prompt("Enter a password");

console.log("Your username is " + username);
console.log("Your password is " + password);

// If col-md or higher then sm shadows under 1st 4 cards + lg shadows under rest
if () {
    console.log();
}

// If col-lg or higher then sm shadows under 1st 3 cards + lg shadows under rest
else {
    console.log();
}